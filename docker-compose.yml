version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: novatopup-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: novatopup
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - novatopup-network

  redis:
    image: redis:7-alpine
    container_name: novatopup-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - novatopup-network

volumes:
  postgres_data:
  redis_data:

networks:
  novatopup-network:
    driver: bridge
